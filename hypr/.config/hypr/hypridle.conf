# hypridle — gestión de inactividad
# Tiempos: 5min → atenuar | 10min → lock | 15min → DPMS off | 30min → suspend
# Inhibidor cuando hay fullscreen idleinhibit (Steam, videos)

general {
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on
    ignore_dbus_inhibit = false
}

# Atenuar brillo a los 5 min
listener {
    timeout = 300
    on-timeout = brightnessctl -s set 10
    on-resume = brightnessctl -r
}

# Bloquear a los 10 min
listener {
    timeout = 600
    on-timeout = loginctl lock-session
}

# Apagar pantalla a los 15 min
listener {
    timeout = 900
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}

# Suspender a los 30 min
listener {
    timeout = 1800
    on-timeout = systemctl suspend
}
